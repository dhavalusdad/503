export const paymentQueryKey = {
  customerProfile: () => ['customer-profile'].filter(item => item !== undefined),
  userPaymentProfile: () => ['user-payment-profile'].filter(item => item !== undefined),
};
